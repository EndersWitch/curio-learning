/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Serve existing HTML files as static assets alongside Next.js routes
  // papers.html, admin.html etc. stay in /public
}
module.exports = nextConfig
